{
  "name": "fantazey-postcards-backend",
  "version": "0.0.0",
  "description": "Simple backend with API for Postcards project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api:start": "pm2 start server.js --name api-server",
    "api:restart": "pm2 restart api-server",
    "api:stop": "pm2 stop api-server",
    "api:list": "pm2 list"
  },
  "author": "Dyatlov Andrew",
  "license": "MIT",
  "dependencies": {
    "body-parser": "1.19.0",
    "express": "4.17.1",
    "method-override": "3.0.0",
    "morgan": "1.9.1",
    "sqlite3": "4.1.1"
  },
  "devDependencies": {
    "pm2": "3.5.2"
  }
}
